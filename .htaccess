# Disable directory listing
Options -Indexes

# Block direct access to config and vendor (Apache 2.4 compatible)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^config/ - [F,L]
    RewriteRule ^vendor/ - [F,L]
</IfModule>
